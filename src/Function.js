/**
 * @param {String} name
 * @param {Number} age
 * @param {Number} limit
 * @param {Object[]} group
 * @returns {Object}
 */
function getPerson(name,age,limit,group) {
    let obj = {};
    obj.name = name;
    obj.age = age;
    return obj;
}
